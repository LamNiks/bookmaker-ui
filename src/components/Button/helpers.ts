import { ButtonVariant } from "./types";

export const isVariant = {
  black: (variant: ButtonVariant) => variant === "black",
  green: (variant: ButtonVariant) => variant === "green",
  blue: (variant: ButtonVariant) => variant === "blue",
};

